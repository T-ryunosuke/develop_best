<% content_for(:title, t('.title')) %>

<div class="container mx-auto mt-10" >
  <div class="flex justify-center">
    <div class="flex-grow max-w-64 md:max-w-md">
      <%= form_with url: login_path do |f| %>
        <div class="py-8 md:mb-8">
          <%= f.label :email, t(".email") %>
          <%= f.text_field :email, class: "input input-bordered input-xs w-full" %>
        </div>

        <div class="md:pb-14">
          <%= f.label :password, t(".password") %>
          <%= f.text_field :password, class: "input input-bordered input-xs w-full form-control" %>
        </div>

        <div class="text-center pt-10 text-thin">
          <%= f.submit t('.login'), class: "btn btn-sm bg-primary-content w-1/3 font-thin md:font-semibold text-gray-700" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
